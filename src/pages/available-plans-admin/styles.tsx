import { styled } from "@mui/material";
import { BaseApp, BaseBox } from "../styles";

export const AvailablePlanBox = styled(BaseBox)({
  maxWidth: "1000px"
});

export const DeletePlanBox = styled(BaseBox)({
  margin: "auto 20px"
});

export const AvailablePlanApp = styled(BaseApp)({
  height: "100vh"
});
